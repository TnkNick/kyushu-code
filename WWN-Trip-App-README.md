# WWN · Japan Trip — Travel Planner App

แอปวางแผนทริปสองภาษา (EN/TH) จากดีไซน์ **WWN · World Wide Nick** · *Designed by 0xTnk*
implement จากไฟล์ดีไซน์ (Claude Design handoff) เป็น **ไฟล์ HTML เดียวที่ deploy ได้จริง** — **ตอนนี้ใส่ข้อมูลทริปฟุกุโอกะ/คิวชู 4–14 ธ.ค. 2026 จริงแล้ว** (11 วัน · ขับรถวนเกาะใต้)

---

## 📦 ไฟล์ที่ได้

| ไฟล์ | คืออะไร |
|---|---|
| **`Japan Trip - App.html`** | ตัวแอปจริง — ไฟล์เดียวจบ พร้อม deploy |
| `wwn-app-source/` | ซอร์สโค้ดแยกโมดูล (`data.jsx`, `components.jsx`, `app.jsx`, `icons.jsx`, `image-slot.js`, `tweaks-panel.jsx`) ไว้อ่าน/แก้ |
| `wwn-app-source/วิธีเตรียมข้อมูล (Content Template).md` | คู่มือกรอกข้อมูลทุก field อย่างละเอียด |
| `images/` | รูปทุกช่องของเว็บ (`images/<id>.jpg`) — ตอนนี้เป็น placeholder · ดูวิธีใส่รูปจริงที่ `images/README.md` |

> หมายเหตุ: ตัวแอปจริงรันจาก `Japan Trip - App.html` (โค้ดทั้งหมด inline อยู่ในไฟล์นี้) — ไฟล์ใน `wwn-app-source/` เป็นสำเนาซอร์สไว้แก้/อ้างอิงเท่านั้น

---

## ▶️ วิธีเปิดดู

- **ง่ายสุด:** ดับเบิลคลิกเปิด `Japan Trip - App.html` ในเบราว์เซอร์ได้เลย
- **หรือเสิร์ฟแบบ local:** `cd` เข้าโฟลเดอร์นี้แล้วรัน
  ```bash
  python3 -m http.server 8080
  ```
  เปิด `http://localhost:8080/Japan%20Trip%20-%20App.html`

⚠️ **ต้องต่อเน็ต** ตอนเปิด เพราะ React / Babel / ฟอนต์ โหลดจาก CDN (เป็นปกติของดีไซน์ตัวนี้)

---

## 🚀 วิธี Deploy บน static web — เป็น PWA ออฟไลน์ได้แล้ว 📱

ตอนนี้แอปทำเป็น **PWA** — deploy แล้วเปิด **ออฟไลน์** + **ติดตั้งลงมือถือ** ได้เหมือนแอปจริง
- **อัปโหลดทั้งโฟลเดอร์** (ไม่ใช่ไฟล์เดียวแล้ว): `index.html` + `vendor/` + `images/` + `manifest.json` + `sw.js` + `icon-192/512.png` + `.nojekyll`
- ขึ้น **GitHub Pages / Netlify / Vercel** โดยตั้งชื่อไฟล์หลักเป็น **`index.html`** (สำเนาของ `Japan Trip - App.html`)
- 📱 เปิดผ่าน browser ครั้งแรก (ตอนมีเน็ต) ตัวแอปจะมี **คำแนะนำ/ปุ่มติดตั้งลงหน้าจอโฮม** ขึ้นเอง · เปิดในโหมด PWA แล้วจะไม่โชว์

---

## ✨ ฟีเจอร์ในแอป (ตามดีไซน์)

- **หน้า Overview** (landing) — ชื่อเกาะ + คันจิลายน้ำ, นาฬิกา real-time 2 โซน (ญี่ปุ่น ⇄ กรุงเทพฯ), **แผนที่เส้นทางแบบ visual** (กดหมุดกระโดดไปวันนั้น), ผู้ร่วมเดินทาง, งบประมาณ (บาท), อัตราแลกเปลี่ยน + ปุ่ม Visit Japan Web, เช็กลิสต์เตรียมตัว, วลีภาษาญี่ปุ่น + ปุ่ม **ฉุกเฉิน (SOS)** กดโทรได้
- **ปุ่ม "เริ่มการเดินทาง" แบบลอย** → เข้าแผนรายวัน
- **Itinerary 10 วัน** — แต่ละวันมีธีมสีของตัวเอง, วันที่เต็มบน header, พยากรณ์อากาศ + ร้านสะดวกซื้อใกล้ที่พักต่อวัน, ไทม์ไลน์ผสม **step** (ไฟลต์/รถไฟ/เช็คอิน) กับ **feature** (สถานที่/ร้านอาหาร รูปใหญ่)
- **รายละเอียดกิจกรรม** — พาเนลเลื่อนจากขวา + **reference dock** (แผนที่/เว็บทางการ/ไกด์/รหัสจอง/โน้ต) · ร้านอาหารมี **เมนูแนะนำ + กดดูรูปอาหาร** ได้
- **หน้า Bookings** — รวมทุกการจอง + เช็กลิสต์ติ๊กได้ + แถบความคืบหน้า (จำค่าไว้)
- **สลับภาษา EN/TH** ทุกข้อความ · **mobile bottom tab bar** · รองรับ reduced-motion · responsive
- **รูปภาพ:** ทุกช่องโหลดรูปจากโฟลเดอร์ `images/` (`images/<id>.jpg`) — ตอนนี้เป็น placeholder มี label บอกว่าช่องไหนคืออะไร · เอารูปจริงเซฟทับชื่อเดิมได้เลย (ดู `images/README.md`) · หรือจะลากไฟล์วางที่ช่องรูปในเบราว์เซอร์ก็ได้

---

## ✏️ วิธีเปลี่ยนเป็น "ทริปจริง" ทีหลัง

ข้อมูลทั้งหมดอยู่ในบล็อกข้อมูลที่ inline ใน `Japan Trip - App.html` (**ประมาณบรรทัด 2489–3444**) — เป็นตัวแปรชุดนี้:

`TRIP` (หัวเรื่อง + วัน + กิจกรรม) · `DESTINATION` (เกาะ + route map) · `TRAVELERS` · `EMERGENCY` · `BUDGET` · `KONBINI` · `WEATHER` · `ESSENTIALS` · `MENUS` · `PACKING` · `PHRASES`

- รูปแบบสองภาษา: `L('English', 'ไทย')`
- รายละเอียดทุก field + ค่าที่เลือกได้ (ธีมสี 6 สี, ประเภทกิจกรรม, ไอคอน ฯลฯ) อยู่ในคู่มือ **`wwn-app-source/วิธีเตรียมข้อมูล (Content Template).md`**
- ⚠️ ถ้าแก้ที่ `wwn-app-source/data.jsx` ต้องก๊อปเนื้อหาไปวางในบล็อกข้อมูลใน HTML ด้วย (เว็บจริงรันจาก HTML)

### ✅ ข้อมูลฟุกุโอกะ — ใส่ในแอปแล้ว (ซอร์ส: `wwn-app-source/data.fukuoka.jsx`)

แปลงแผน **ฟุกุโอกะ/คิวชู 4–14 ธ.ค. 2026** เป็นข้อมูลตาม template ครบทุก field และ **ซิงค์เข้า `Japan Trip - App.html` แล้ว** (ผ่าน validate: 11 วัน · 58 กิจกรรมรูปใหญ่ · placeholder ครบ 155 ช่อง · อากาศ/konbini ครบทุกวัน · 24 เมนูร้าน · งบ ฿135,000 — ไม่มี error)

**ถ้าจะแก้/ใส่ข้อมูลใหม่อีกครั้ง:** แก้ที่ `data.fukuoka.jsx` แล้วก๊อปทั้งไฟล์ไปแทนบล็อก data ใน `Japan Trip - App.html` (บล็อก `<script type="text/babel">` ที่ขึ้นต้นด้วย `const L = (en, th) =>` จนถึงบรรทัด `Object.assign(window, {...})`)

> รูปภาพมี placeholder ครบทุกช่องในโฟลเดอร์ `images/` แล้ว (เอารูปจริงเซฟทับชื่อเดิมได้เลย — ดู `images/README.md`) · มีไฟล์สำรอง `Japan Trip - App.html.bak` (เวอร์ชัน Kyoto เดิม) เผื่ออยากย้อนกลับ — ลบทิ้งได้ถ้าไม่ต้องการ

---

## 🔧 หมายเหตุการ implement

- ไฟล์นี้ **ตรงกับดีไซน์ทุกพิกเซล** (เป็นไฟล์เดียวกับที่ออกแบบไว้ ผ่านการ verify ในเครื่องมือดีไซน์แล้ว)
- แก้เพิ่มอย่างเดียว: เปลี่ยน `<title>` ของแท็บเบราว์เซอร์จากค่าค้างเก่า *"Itinéraire — Kyoto Lookbook"* → **"WWN · Japan Trip"** ให้ตรงกับตัวแอป
- สถาปัตยกรรม: React 18 (UMD) + Babel standalone แปลง JSX ในเบราว์เซอร์ + ฟอนต์ Google — โหลดจาก CDN ทั้งหมด, โค้ดแอปทั้งหมด inline ในไฟล์ HTML
